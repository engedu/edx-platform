define(['backbone'],
    function(Backbone) {
        var NotificationDetail = Backbone.Model.extend({
            defaults: {
            }
        });

        return NotificationDetail;
    }); // end define()
